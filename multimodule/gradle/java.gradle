allprojects {

    apply {
        plugin('java')
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of("${javaVersion}")
        }
        withSourcesJar()
        withJavadocJar()
    }

    configurations {
        compileOnly {
            extendsFrom annotationProcessor
        }
    }
}